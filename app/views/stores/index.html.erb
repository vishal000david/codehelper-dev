 <div class="col-sm-2">
    <%= render partial: "folders/save_folder_modal" %>

  </div>

  <button type="button" class="btn btn-default" data-toggle="modal" data-target="#saveFolder"><span class="glyphicon glyphicon-upload"></span>New Folder</button>


  <% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>

<p id="notice"><%= notice %></p>

<h1>All files</h1>


<table class="table table-hover table-striped">
      <thead>
        <tr class="sortable">
          <th><input type="checkbox" value="" ng-model="FM.selectAll"></th>
          <th>Type</th>
          <th>Name</th>
          <th>Size</th>
          <th>Time</th>
        </tr>
      </thead>
      <tbody>
      <tr class="sortable" ng-repeat="file in FM.curFiles | orderBy:'folder'">
        <td><input type="checkbox" value="{{file.name}}" ng-model="file.selected"></td>
        <td> Folder </td>
        <td >
        <%= link_to 'Builder', select_folder_path(:type => "Builder") %>
        </td>
        <td></td>
        <td></td>
      </tr>
      

            <tr class="sortable" ng-repeat="file in FM.curFiles | orderBy:'folder'">
        <td><input type="checkbox" value="{{file.name}}" ng-model="file.selected"></td>
        <td> Folder </td>
        <td >
         <%= link_to 'File Manager',                             select_folder_path(:type => "FileManager") %>
        </td>
        <td></td>
        <td></td>
      </tr>

        <tr class="sortable" ng-repeat="file in FM.curFiles | orderBy:'folder'">
        <td><input type="checkbox" value="{{file.name}}" ng-model="file.selected"></td>
        <td> Folder </td>

        <td >
        <%= link_to 'IDE', select_folder_path(:type => "IDE") %>
        	
        </td>
        <td></td>
        <td></td>
      </tr>
            <tr class="sortable" ng-repeat="file in FM.curFiles | orderBy:'folder'">
        <td><input type="checkbox" value="{{file.name}}" ng-model="file.selected"></td>
        <td> Folder </td>
        <td >
        <%= link_to 'SnippetManager', select_folder_path(:type => "SnippetManager") %>
        	
        </td>
        <td></td>
        <td></td>
      </tr>


        
      </tr>
      

      </tbody>
    </table>
    




