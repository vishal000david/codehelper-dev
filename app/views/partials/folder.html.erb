<table class="table table-hover table-striped">
      <thead>
        <tr class="sortable">
          <th>File</th>
      <th>Size</th>
      <th>Uploaded to</th>
       
       <th class="text-center"> <td> Actions </td></th>

        </tr>
        </thead>

        <tbody>
    <% @store_list.each do |store| %>
      <tr>
        <td><%=  File.basename(store.upload.path) %>
         </td>
        <td><%= store.upload.size %> bytes</td>
        <td><%= store.folderType %></td>

        <th>
        <td><a href="<%= store.upload %>" download> download </a></td>
        <td data-toggle="modal" data-target="#myModal">Share File</td>

        <td><%= link_to 'Show', store %></td>
        <td><%= link_to 'Edit', edit_store_path(store) %></td>
        <td><%= link_to 'Destroy', store, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </th>


      </tr>
    <% end %>
  </tbody>
</table>


  <!-- Modal -->
  <div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog">
       <div class="modal-content">
         <div class="modal-header">
           <button type="button" class="close" data-dismiss="modal">&times;</button>
           <h4 class="modal-title">Share File with other user</h4>
         </div>

        <div class="modal-body"> 
        <%= form_for @share ,:url => give_perm_path do |f| %>
 					 <div class="form-group">
            Share with:
            <%=f.collection_select :reciever_id,@except_current, :id, :name %>
            </div>


            <div class="form-group">
            <label class="control-lablel"> label</label>
             Read <input type="checkbox" name="share[permission][]" value="R">
             Write <input type="checkbox" name="share[permission][]" value="W">
             Execute <input type="checkbox" name="share[permission][]" value="X">
             </div>




            <%= f.submit "Give Permission"%>
          <% end %>
        </div>

        <table class="table table-hover table-striped">
      
      <thead>
        <tr class="sortable">
          <th>File</th>
      <th>Size</th>
      <th>Uploaded to</th>
       
       <th class="text-center"> <td> Actions </td></th>

        </tr>
        </thead>

        <tbody>
      <tr>
        <td> download </a></td>


      </tr>
  </tbody>
</table>





        <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
        </div>
      </div>
      
    </div>
  </div>
  



